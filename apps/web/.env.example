NEXT_PUBLIC_API_URL=http://localhost:3000

# Must match SESSION_COOKIE_NAME in apps/api/.env — middleware.ts reads this
# cookie to decide whether to redirect to /login before rendering.
# Only works if web and api share the same top-level domain in every
# environment (cookies are shared by host, not by port).
SESSION_COOKIE_NAME=arch_session
